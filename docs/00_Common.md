## Общие требования (для всех задач)

- Бот работает в **супергруппе Telegram с включённым форумом** (Topics).
- Бот обслуживает **только одного пользователя** (`ALLOWED_USER_ID`) — все остальные апдейты игнорируются без побочек.
- Бот умеет:
  - В личке: `/mkproj` → выбрать директорию → создать Topic в группе.
  - В Topic: текст → запускает `codex` в `cwd=project_dir`, стримит вывод, ловит approval prompt, обновляет title.
  - Команды в Topic: `/status`, `/cancel`, `/new`.
- Никаких очередей: если job идёт — новые сообщения игнорируются (кроме команд).
- Вывод режем по 4096 символов (лимит Telegram).
- Никаких секретов в логах. Секреты — как Windows Registry: *кажется, что удалил, но оно всё равно где-то лежит* 😼.
