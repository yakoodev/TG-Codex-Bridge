# ТЗ: Telegram ↔ Codex CLI интеграция (1 Topic = 1 директория проекта)

Дата: 2026-02-16 (Europe/Berlin)  
Проект: **tg-codex-bridge** (рабочее имя)

## 0. Цель

Сделать Telegram-бота, который управляет **Codex CLI** и организует работу так, чтобы:

- **Одна супергруппа-форум** (Topics включены) — основной “хаб”.
- **Каждый Topic (тема)** в супергруппе **жёстко соответствует одной директории проекта** на хосте:  
  `Topic = project directory`.
- Любое обычное сообщение пользователя в Topic запускает обработку через Codex CLI для этой директории.
- Если у Topic уже есть сохранённая сессия Codex — бот автоматически делает **/resume** для неё.
- **/new** создаёт **новый Codex-чат** в *той же директории*, и в Telegram это отражается созданием **нового Topic**.
- Стриминг **не нужен** (и его эмуляция не нужна), но пользователь должен понимать, что происходит: бот публикует понятные статусные сообщения и вывод Codex блоками.
- Любые сообщения пользователя во время выполнения задачи **игнорируются** (без очередей), кроме команд (например **/cancel**).

> Делаем “как в Codex”, но в Telegram, чтобы не получилось «всё работает, но хуй пойми что» 😼

---

## 1. Термины

- **TG Group** — супергруппа-форум, где Topics включены.
- **Topic / Thread** — тема в форуме (message_thread_id).
- **Project Directory** — путь на диске (смонтированный в Docker), привязанный к Topic.
- **Codex Chat** — “чат/сессия” в Codex CLI, которую можно **/resume**.
- **Job** — запуск Codex CLI на одно пользовательское сообщение (или команду), ведущий к результату или вопросу.

---

## 2. Обязательные ограничения и политика доступа

### 2.1 Allowlist по TG user id (строгая)
- Бот обслуживает **строго одного** пользователя: `ALLOWED_USER_ID`.
- Для любых сообщений от других пользователей бот **не отвечает** и **ничего не делает**.

### 2.2 Безопасность путей
- Бот работает только в директориях, доступных внутри Docker (смонтированные пути).
- Разрешены только пути, которые лежат внутри `ALLOWED_ROOTS` (whitelist корней).
- **Обязательно**: перед любыми файловыми операциями:
  1) нормализовать путь (убрать `..`, лишние разделители),
  2) получить **реальный путь** (resolve/realpath с учётом симлинков),
  3) проверить, что `realpath` **начинается** с одного из `ALLOWED_ROOTS` (тоже в realpath-виде).
- Если проверка не прошла — отказ с сообщением в чат (без выполнения Codex).

> Примечание про пути: внутри контейнера используется POSIX-путь (например `/host/C/Users/...`).  
> Если хочется показывать “красивый” Windows-вид (`C:\Users\...`) — это **только отображение**, хранение и проверки делаем по docker-пути.


---

## 3. Telegram модель

### 3.1 Супергруппа-форум
- В группе **только пользователь и бот** (на старте).
- Бот — **админ** с правом управления темами (manage topics).
- Topics включены.

### 3.2 Правило соответствия
- **Один Topic = одна директория проекта**.
- Название Topic содержит 4 части (в указанном порядке):

1) **Статус** (emoji)  
2) **Название**  
3) **Остаток контекста** в %  
4) **Директория**

Шаблон названия:

- Idle: `🟢 {Name} · {ContextLeft}% · {Dir}`
- Working: `🟡 {Name} · {ContextLeft}% · {Dir}`
- Error/Cancelled: `🔴 {Name} · {ContextLeft}% · {Dir}`

Где:
- `Name` — человекочитаемое имя (например, имя папки / проект).
- `ContextLeft` — число 0–100 (если неизвестно — `??`).
- `Dir` — полный путь (например `C:\Users\Yakov.M\source\repos\github\ModelRouter`).


Ограничения на заголовок Topic (чтобы Telegram не устроил «я не помещаюсь»):
- `Dir` в заголовке **не обязан быть полным**: показываем хвост пути (например последние 2–3 сегмента), а **полный путь** храним в БД.
- Если `{Name}` слишком длинный — обрезаем и добавляем `…`.
- Переименование темы (rename) делать с **debounce** (например не чаще 1 раза в 1–2 секунды), чтобы не упереться в лимиты Telegram.


### 3.3 Команды в Topic
- Обычное сообщение (не команда) → запускает Codex Job.
- Команды всегда обрабатываются (в т.ч. когда job выполняется).

Минимальный набор команд в Topic:
- `/cancel` — отменить текущую задачу (если выполняется).
- `/new` — создать новый Codex-чат в этой же директории и **создать новый Topic**.
- (опционально) `/status` — показать состояние: idle/working, текущая команда/шаг, сохранённый codex_chat_id.

---

## 4. Логика выполнения задач (Jobs)

### 4.1 Общая схема запуска
Когда пользователь отправляет обычный текст в Topic:

1) Проверить allowlist `from.id`.
2) Определить `thread_id` (message_thread_id) и найти привязку `thread_id -> project_dir`.
3) Проверить `busy` для этого `thread_id`.
4) Если `busy == true`:
   - Если сообщение **не команда**:  
     - Ответить реплаем:  
       `⏳ Сообщение проигнорировано: выполняется задача. Доступны команды, например /cancel.`
     - Сообщение не ставится в очередь и не будет выполнено позже.
   - Если команда: обработать команду.
5) Если `busy == false`:
   - Установить `busy=true`.
   - Обновить название Topic: статус `🟡`.
   - Запустить Codex CLI в `project_dir`.
   - Если для Topic есть сохранённый `codex_chat_id`:
     - Автоматически выполнить `/resume` для этого чата (см. раздел 8).
   - Выполнить задачу пользователя.
   - Публиковать вывод Codex блоками в виде отдельных сообщений (см. раздел 5).
   - На завершении:
     - обновить `ContextLeft%` (см. раздел 7),
     - обновить название Topic на `🟢` (или `🔴` если ошибка/отмена),
     - установить `busy=false`,
     - завершить Codex процесс.

### 4.2 Отмена /cancel
- `/cancel` должен работать даже если `busy=true`.
- Реализация: отправить в Codex stdin нужный сигнал/команду (или убить процесс), дождаться завершения.
- После отмены:
  - `busy=false`,
  - статус Topic `🔴` или `🟢` (решить и зафиксировать в реализации; по умолчанию `🔴`),
  - сообщение в чат: `🛑 Отменено пользователем.`

### 4.3 Исключение очередей
- Очередь пользовательских сообщений **не делаем**.
- Во время работы любые обычные сообщения пользователя игнорируются с ответом “проигнорировано”.

---

## 5. Схема сообщений в Telegram (без стриминга)

### 5.1 Принцип разбиения
На каждый процесс/логическое разделение/важный блок вывода Codex бот создаёт **отдельное сообщение** в Topic.

Обязательно (MVP):
- Чтение stdout/stderr Codex — **инкрементально** (строки/чанки по мере поступления), без “прочитаем всё в конце”.
- Любое исходящее сообщение в Telegram должно укладываться в лимит (≈4096 символов):  
  если блок длиннее — **разбиваем на части** и отправляем несколькими сообщениями с понятным суффиксом (`(1/3)`, `(2/3)`, ...).


Примеры отдельных сообщений:
- `🛠 Codex запущен (job #123)`
- `↩️ Resume: <codex_chat_id>`
- `💻 Команда: Remove-Item ...`
- `🧠 План: ...`
- `✅ Завершено: ...`
- `❌ Ошибка: ...`

### 5.2 Вопросы/подтверждения — отдельное сообщение + inline кнопки + уведомление
Когда Codex задаёт вопрос/просит подтверждение:
- Бот отправляет отдельное сообщение, содержащее:
  - текст вопроса,
  - команду (если есть),
  - inline кнопки (Yes / Yes always / No),
  - **упоминание** пользователя(ей) из списка уведомлений.

### 5.3 Завершение задачи — отдельное сообщение + уведомление
Когда Codex выдаёт “заключение/итог” (conclusion), бот:
- отправляет отдельное сообщение с итогом,
- добавляет упоминание notify-пользователей.

> В Telegram нет универсального @all, поэтому уведомление делается через упоминания конкретных user id (см. раздел 6).

---

## 6. Уведомления (notify)

### 6.1 Список уведомляемых
- `NOTIFY_USER_IDS` — список user_id для пинга в важных случаях.
- По умолчанию содержит `ALLOWED_USER_ID`.

### 6.2 Когда пингуем
- При возникновении вопроса/подтверждения от Codex (approval prompt).
- При завершении задачи (conclusion).
- (опционально) при ошибке.

### 6.3 Формат упоминания
- Использовать кликабельное упоминание пользователя по user id (tg://user?id=...), чтобы уведомление сработало независимо от наличия username.

---

## 7. Остаток контекста (ContextLeft%)

### 7.1 Требование
В названии Topic должен отображаться `ContextLeft%` как в Codex (например “72% context left”).

### 7.2 Источник данных (приоритет)
**Приоритет 1 (быстро):** парсить текстовую строку вида:
- `{N}% context left`

Regex:
- `(?i)\b(\d{1,3})\s*%\s*context\s+left\b`

**Приоритет 2 (fallback):** если не найдено — оставлять `??`.

### 7.3 Когда обновлять
- При старте job: оставить предыдущее значение или `??`.
- При завершении job: обновить на последнее найденное значение.
- (опционально) после завершения можно вызвать короткую команду `/status` внутри codex, если это не ломает UX и не тратит лишние лимиты.

---

## 8. Codex CLI: /resume, /new, approvals

### 8.1 Требование: /resume автоматически
- Если для Topic есть сохранённый `codex_chat_id`, то при старте Codex процесса бот автоматически выполняет `/resume <id>` (или эквивалентный синтаксис Codex CLI), затем отправляет задачу пользователя.
- `codex_chat_id` должен обновляться, если в выводе Codex встречается новый/уточнённый id (парсинг по regex/эвристикам).


### 8.2 /new
- В Codex `/new` создаёт новый чат (новую беседу) в рамках той же директории.
- В Telegram команда `/new` **создаёт новый Topic** (новая тема) с тем же `project_dir`.
- Новый `codex_chat_id` для этого Topic:
  - **предпочтительно парсится** из вывода Codex после `/new` (regex/эвристика по “id/chat id” строкам),
  - если распарсить не удалось — выполняем `/resume`, берём **самый верхний/последний созданный** чат из списка и сохраняем его id,
  - если и это не дало id — сохраняем `codex_chat_id = NULL` и продолжаем (следующий успешный парсинг обновит запись).


### 8.3 /resume список чатов
- В Codex `/resume` выводит список ранее открытых чатов.
- В рамках TG-бота список пользователю не обязателен; предпочтительно хранить `codex_chat_id` в БД и резюмить напрямую.
- Если нужно выбирать чат из списка (fallback сценарий):
  - бот парсит `/resume`-листинг и берёт верхний/последний созданный,
  - опционально может показать пользователю 5 последних и дать кнопки выбора (не обязательно для MVP).


---

## 9. Approval prompt (подтверждение команд) — парсинг и кнопки

### 9.1 Пример prompt
```
Would you like to run the following command?

$ Remove-Item tasks\todo\004-pin-dotnet-sdk-with-global-json.md -Force

› 1. Yes, proceed (y)
  2. Yes, and don't ask again for commands that start with `Remove-Item ...` (p)
  3. No, and tell Codex what to do differently (esc)
```

### 9.2 Детект prompt
Считаем, что prompt начался, если встречаем строку:
- `Would you like to run the following command?`

Далее извлекаем:
- команду из строки, начинающейся с `$ `.

### 9.3 Inline кнопки и ответы в stdin
Кнопки:
- ✅ Yes (y) → отправить `y\n`
- ✅✅ Yes always (p) → отправить `p\n`
- ❌ No (esc) → отправить ESC (`\x1b`), при необходимости fallback `\x1b\n`

### 9.4 Поведение при prompt
- Бот отправляет отдельное сообщение с текстом вопроса + командой.
- Добавляет упоминание notify пользователей.
- При нажатии кнопки:
  - **фиксирует выбор** (в памяти job + в БД как event),
  - **редактирует** сообщение с кнопками: показывает “Выбрано: Yes/Yes always/No” и **убирает/деактивирует** кнопки (защита от дабл-клика),
  - продолжает выполнение Codex,
  - публикует последующие блоки отдельными сообщениями.


---

## 10. Личка с ботом: создание проектов из директорий (UX)

### 10.1 Требование
В личке бот предоставляет UX для выбора директорий на хосте (смонтированных в Docker), и позволяет сделать “проект” из любой директории, создав соответствующий Topic в супергруппе.

### 10.2 UX вариант A: интерактивное дерево (основной)

Ограничения “по вайбу, чтобы не сожрать весь диск” (MVP):
- Пагинация: `PAGE_SIZE` (по умолчанию 30).
- Максимум показываемых элементов в одной директории: 300 (если больше — показываем первые 300 + сообщение “слишком много, уточни путь”).
- Не заходим рекурсивно: дерево строится **только на 1 уровень** за запрос (только текущая папка).
- Запрещаем/скрываем системные/мусорные директории по маске (минимум): `.git`, `node_modules`, `bin`, `obj`, `Library`, `Temp`.
- Все проверки путей — через правила из раздела 2.2.

Команда: `/mkproj`

- Бот отправляет одно сообщение “Выбор директории”.
- В сообщении отображается:
  - текущий путь (breadcrumb),
  - список подпапок (постранично),
  - кнопки управления.

Кнопки:
- `⬆️ Up`
- `◀️ Prev` / `Next ▶️`
- `📂 Open <folder>`
- `✅ Select this folder` (создать проект)
- `🔎 Search`
- `⭐ Favorites`
- `🕘 Recent`
- `✍️ Enter path` (перейти к варианту B)

После каждого клика бот **редактирует** это же сообщение (интерактивно, без спама новыми сообщениями).

### 10.3 UX вариант B: ввод пути (быстрый)
- Пользователь отправляет путь текстом (или через кнопку “Enter path”).
- Бот валидирует путь (существует, директория, внутри allowed roots).
- Предлагает кнопку `✅ Select this folder`.

### 10.4 Создание проекта (результат)
После выбора директории:
- Бот создаёт Topic в супергруппе с названием по шаблону:
  `🟢 {Name} · ??% · {Dir}`
- Привязывает `thread_id` к `project_dir`.
- Создаёт/инициализирует `codex_chat_id` при первом сообщении в Topic (или сразу — на усмотрение реализации).
- Возвращает пользователю в личку подтверждение и (опционально) ссылку на топик.

---

## 11. Хранение данных (SQLite)

### 11.1 Минимальные таблицы
**projects**
- project_id (PK)
- dir_path (unique)
- name
- created_at

**topics**
- topic_id (PK)
- group_chat_id
- message_thread_id (unique)
- project_id (FK)
- codex_chat_id (nullable)
- context_left_percent (nullable int)
- status (idle/working/error)
- title_base (nullable)
- busy (bool)
- last_job_started_at
- last_job_finished_at

**notify_users**
- user_id (PK)

**audit_log** (опционально)
- id, ts, topic_thread_id, event_type, payload

### 11.2 Важно
- `codex_chat_id` хранится на уровне Topic.
- `/new` создаёт новый Topic → новый row в `topics` с новым `codex_chat_id`.

---

## 12. Docker упаковка

### 12.1 Цель
Сборка должна быть переносимой: любой сможет поднять, подставив:
- свой `BOT_TOKEN`,
- свой `ALLOWED_USER_ID`,
- свой `GROUP_CHAT_ID`,
- примонтировав нужные директории.

### 12.2 Переменные окружения
- `BOT_TOKEN`
- `ALLOWED_USER_ID`
- `GROUP_CHAT_ID`
- `ALLOWED_ROOTS` (например `/host/C/Users;/host/home`)
- `STATE_DIR=/data`
- `CODEX_BIN=codex`
- (опционально) `CODEX_CONFIG_DIR=/data/codex` (если нужен доступ к авторизации/конфигу codex)

### 12.3 Volumes
- `./data:/data`
- `/path/on/host:/host/...` (маунты директорий)
- (опционально) маунт конфигов codex

---

## 13. Критерии приёмки (Acceptance Criteria)

### 13.1 Telegram/Topics
- [ ] Бот работает только для `ALLOWED_USER_ID` и молчит для остальных.
- [ ] Бот умеет создавать Topic в супергруппе и переименовывать Topic по шаблону статуса.
- [ ] Название Topic всегда содержит: **статус + имя + % + директория**.

- Название Topic не превышает ограничения Telegram: длинный `{Dir}` сокращается до хвоста пути, полный путь хранится в БД.
- Переименование темы не спамит (есть debounce).

### 13.2 Создание проекта из лички
- [ ] `/mkproj` открывает интерактивный “проводник” (одно сообщение редактируется).
- [ ] Можно выбрать директорию кликами и создать Topic.
- [ ] Можно ввести путь вручную и создать Topic.
- [ ] Выбор ограничен `ALLOWED_ROOTS`.

### 13.3 Выполнение задач
- [ ] Обычное сообщение в Topic запускает Codex CLI в нужной директории.
- [ ] Если есть `codex_chat_id`, бот делает `/resume` автоматически.
- [ ] Пока задача выполняется, любые обычные сообщения игнорируются с ответом “проигнорировано…”.
- [ ] Команды (особенно `/cancel`) работают во время выполнения.
- [ ] По завершении Codex процесс завершается (не остаётся висящих процессов).

- Длинный вывод Codex разбивается на сообщения (лимит Telegram не нарушается).

### 13.4 Approval prompt
- [ ] При появлении prompt “Would you like to run the following command?” бот распознаёт его.
- [ ] Бот публикует отдельное сообщение с командой и кнопками Yes/Yes always/No.
- [ ] При нажатии кнопок в Codex stdin уходит корректный ввод (y/p/esc).
- [ ] На вопрос и на conclusion бот пингует notify пользователей.

- Сообщение с prompt после выбора редактируется (кнопки убраны/деактивированы, видно выбранный вариант).

### 13.5 ContextLeft%
- [ ] Бот парсит строку `NN% context left` и обновляет значение в названии Topic после job.
- [ ] Если значение не найдено — отображается `??%` без падений.

### 13.6 /new
- [ ] Команда `/new` создаёт новый Topic с тем же `project_dir`.
- [ ] Новый Topic привязан к новому `codex_chat_id` (новый чат в Codex).
- [ ] Старый Topic остаётся без изменений.

---

## 14. Набор “необязательных, но желательных” улучшений (не блокируют MVP)
- [ ] Показывать в итоговом сообщении `git status` и `git diff --stat` (если директория — git repo).
- [ ] Лимиты на размер сообщений: обрезка длинных блоков вывода с пометкой “обрезано”.
- [ ] Логи в файлы `/data/logs/...` для отладки.
- [ ] Поддержка нескольких корней и избранных директорий.

---

## 15. План реализации (по этапам)

1) **Telegram skeleton**: allowlist, обработка лички vs топиков, работа с manage topics.
2) **SQLite state**: topics/projects/notify_users.
3) **UX /mkproj**: дерево + редактируемое сообщение + ввод пути.
4) **Job runner**: запуск Codex CLI в рабочей директории; busy-логика; игнор сообщений.
5) **Parser**: блоки вывода, approval prompt, context left.
6) **Inline callbacks**: кнопки y/p/esc → stdin.
7) **Topic title updater**: 🟢/🟡/🔴 + % + dir.
8) **/new**: новый Topic + новая codex-сессия.

---

## 16. Что считать “готово” (Definition of Done)
- Все acceptance criteria из раздела 13 выполнены.
- Docker image собирается и запускается по инструкции.
- Бот стабильно обрабатывает минимум 30 последовательных задач без утечек процессов.
- При падении Codex/ошибке бот корректно переводит Topic в 🔴 и сообщает причину.

## Implementation note (2026-02-17)
Actual status is tracked in `docs/tasks/STATUS.md`.
If behavior differs from this specification, trust status file and code as source of truth.
