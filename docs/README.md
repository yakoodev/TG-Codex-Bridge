# TG↔Codex Bridge — задачи для агента (C#)

Набор задач для реализации Telegram-бота (C#), который в супергруппе-форуме ведёт топики, запускает `codex` в выбранной директории и обновляет название топика статусом/контекстом.

**Ключевые решения (по уточнениям):**
- Язык: **C#**
- Запуск Codex CLI: командой **`codex`**
- Отмена: **мягкая**, если возможно (через stdin/сигнал), затем **kill по таймауту**
- Доступ к путям: по умолчанию **все диски/всё дерево**, но *в конфиге оставляем выключатель*, чтобы при желании можно было вернуть allowlist (потому что “дать всё” — это как дать root коту: весело ровно до первого пожара 🔥😼).
- `audit_log` — опционально (“если будет время”)

## Структура задач
Папки: `A/`, `B/`, ... — блоки. Внутри — отдельные md-файлы задач.

## Соглашения
- Каждая задача содержит: **Цель → Сделать → Критерии приёмки → Как проверить**.
- Логи и state храним в `/data` внутри контейнера.

## Current implementation status
See `docs/tasks/STATUS.md` for actual done/todo state after recent changes.
