# B2 — DAO/Repositories: операции состояния

## Цель
Сделать слой данных, чтобы бизнес-логика не лезла в SQL напрямую (а то будет “спагетти по-DevOps’овски” 🍝).

## Сделать
- Репозитории/методы:
  - `GetOrCreateProject(dirPath)`
  - `CreateTopic(projectId, groupChatId, threadId, name)`
  - `GetTopicByThreadId(groupChatId, threadId)`
  - `SetTopicBusy(topicId, busy)`
  - `UpdateTopicStatus(topicId, status)` (`idle|working|error|cancelled`)
  - `UpdateTopicContextLeft(topicId, percent?)`
  - `UpdateTopicCodexChatId(topicId, codexChatId?)`
  - `ListNotifyUsers()`
- Транзакции для “старт job” и “финиш job”.
- (опц.) запись в `audit_log` при важных событиях.

## Критерии приёмки
- Все операции работают и не рвутся на конкурентном доступе одного потока/процесса.

## Как проверить
- Мини-тесты в `TgCodexBridge.Tests`:
  - создать проект/топик → получить → обновить поля → проверить.
