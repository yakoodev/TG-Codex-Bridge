# F3 — `/cancel`: мягкая отмена + kill по таймауту

## Цель
Останавливать текущий job максимально “по-людски”, а если codex тупит — добивать по таймауту (как зомби-процесс: “живой, но уже мешает” 🧟‍♂️).

## Сделать
- При `/cancel`:
  1) Попытаться мягко:
     - отправить в stdin команду отмены (если codex поддерживает). Реализация через конфиг:
       - `CANCEL_SOFT_COMMAND` (по умолчанию пусто)
       - варианты: `\x03` (Ctrl+C) **или** специальная команда, если есть.
  2) Подождать `CANCEL_SOFT_TIMEOUT_SEC`.
  3) Если не завершился:
     - `process.Kill(entireProcessTree: true)`
     - подождать `CANCEL_KILL_TIMEOUT_SEC`
- После отмены:
  - `busy=false`
  - status=`cancelled`
  - title → `🔴 ...`
  - сообщение: `🛑 Отменено пользователем.`

## Критерии приёмки
- Отмена срабатывает всегда.
- После отмены можно запускать следующий job.

## Как проверить
- Запустить “долгую” задачу → `/cancel` → процесс останавливается, busy сбрасывается.
