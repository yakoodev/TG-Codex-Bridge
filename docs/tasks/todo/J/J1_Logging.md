# J1 — Логи и “не палим секреты”

## Цель
Нормальные логи без утечек токенов, чтобы потом не было “ой, это не я, это кот нажал deploy” 🐈‍⬛.

## Сделать
- Логи в `LOG_DIR`:
  - start/stop бота
  - start/end job
  - cancel
  - approval choice
  - ошибки процесса
- Маскирование:
  - `BOT_TOKEN` не логировать вообще
  - если в выводе codex есть “Bearer …” или похожее — замазать (regex)

## Критерии приёмки
- В логах нет BOT_TOKEN и похожих секретов.

## Как проверить
- Поиск по логам `grep -R "BOT_TOKEN\|Bearer\|sk-" ./data/logs`
