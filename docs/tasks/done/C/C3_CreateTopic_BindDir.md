# C3 — Создание Topic в группе и привязка “topic ↔ directory”

## Цель
После выбора директории создать topic в супергруппе и сохранить привязку в БД.

## Сделать
- Использовать `GROUP_CHAT_ID`
- Создать topic (forum topic) через Telegram API.
- Сохранить:
  - `projects.dir_path`
  - `topics.group_chat_id`, `topics.message_thread_id`, `topics.project_id`
- Инициализировать title:
  - `🟢 {Name} · ??% · {DirTail}`
  - `Name` = имя папки
  - `DirTail` = “хвост” пути (например последние 1–2 сегмента)
- Отправить в topic приветственное сообщение: как запускать, команды.

## Критерии приёмки
- Topic создаётся и отображается в группе.
- В БД появился `project` и `topic`.
- Title соответствует формату.

## Как проверить
- Выбрать папку → увидеть новый topic в группе.
- `sqlite3 state.db` проверить записи.
