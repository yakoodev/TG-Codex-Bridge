# D1 — Title updater: статус/контекст/директория + debounce

## Цель
Переименовывать topic, показывая статус и остаток контекста, без тележной истерики по rate limit.

## Сделать
- Генератор заголовка:
  - Idle: `🟢`
  - Working: `🟡`
  - Error/Cancelled: `🔴`
  - Формат: `{emoji} {Name} · {ContextLeft}% · {DirTail}`
- Debounce: обновлять не чаще 1 раза в 2 секунды.
- Обрезка длины до лимитов Telegram (перестраховаться: < 120 символов).
- Триггеры:
  - start job
  - end job
  - найден `NN% context left`
  - cancel/error

## Критерии приёмки
- На старте job title становится 🟡.
- По завершении возвращается 🟢 и обновляет %.
- При частых апдейтах не “ддосит” rename.

## Как проверить
- Запустить job → смотреть на смену emoji.
- Сымитировать 10 апдейтов context → rename не чаще debounce.
