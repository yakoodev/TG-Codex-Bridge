# E1 — Job runner: запуск `codex` в cwd + потоковый вывод

## Цель
Запускать `codex` в директории проекта и стримить вывод в Telegram.

## Сделать
- Spawn процесса:
  - `FileName=CODEX_BIN` (по умолчанию `codex`)
  - `WorkingDirectory=project_dir`
  - Redirect stdout/stderr/stdin
- Потоковый ридер:
  - читать построчно (или чанками)
  - копить буфер и отправлять в TG кусками до 4096 символов
- Статусные сообщения:
  - `🛠 Codex запущен`
  - `✅ Завершено`
  - `❌ Ошибка` + краткий хвост stderr
- Busy-логика:
  - если `topics.busy=true` и входящее не команда → ответить “проигнорировано” и выйти.

## Критерии приёмки
- `codex` реально запускается в нужной папке (можно проверить по создаваемым файлам).
- Длинный вывод корректно режется и отправляется.
- Во время busy обычные сообщения не стартуют новый процесс.

## Как проверить
- В topic отправить задачу, которая создаёт файл → файл появляется в директории.
- Сгенерить большой вывод → проверить нарезку.
