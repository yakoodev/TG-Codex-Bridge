# F2 - Parse `NN% context left` (Done)

## Status
Done in code.

## Implemented
- Regex parser for `NN% context left` is implemented.
- Parsed value is saved into `topics.context_left_percent`.
- Topic title updates with parsed percent.
- Fallback `??%` remains when value is not found.
